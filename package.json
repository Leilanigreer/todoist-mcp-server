{
  "name": "todoist-mcp-server",
  "version": "0.1.0",
  "description": "MCP server for Todoist integration via Streamable HTTP",
  "type": "module",
  "main": "dist/mcp-http-server.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/mcp-http-server.js",
    "dev": "tsx src/mcp-http-server.ts",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "axios": "^1.6.0",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "files": [
    "dist/**/*"
  ]
}